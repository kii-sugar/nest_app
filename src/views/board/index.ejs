<!DOCTYPE html>
<html lang="ja">
  <head>
		<meta charset="UTF-8" />
		<title>Board page</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
	</head>
	<body>
		<h1 class="bg-info text-white p-2">Board page</h1>
		<div class="container py-2">
			<h2 class="mb-3"><%= msg %></h2>
			<div class="alert alert-info">
				<form method="post" action="/board/">
					<div class="mb-2">
						<label>ユーザー名: </label>
						<h6><%= login.name + '(' + login.email +')' %></h6>
            <input type="hidden" name="mydata" value="<%= login.id %>" />
					</div>
					<div class="mb-2">
						<label>メッセージ: </label>
						<input type="text" name="message" class="form-control" />
					</div>
					<div>
						<input type="submit" value="投稿" class="btn btn-info" />
					</div>
				</form>
			</div>
			<table class="table my-4">
				<thead>
					<tr>
						<th>message</th>
						<th>name</th>
					</tr>
				</thead>
				<tbody>
					<%
						for (let i in data) {
							let item = data[i]
					%>
							<tr>
								<td><%= item.message %><br> (<%= new Date(item.date).toLocaleString() %>)</td>
								<td><%= item.mydata!= undefined ? item.mydata.name : "-" %></td>
							</tr>
					<% } %>
				</tbody>
			</table>
		</div>
	</body>
</html>